/**
 * ⚠️ PLACEHOLDER TESTIMONIALS — REPLACE BEFORE LAUNCH.
 * Paste your real, verbatim Upwork / client reviews here (with permission).
 * Use real names + roles where allowed; quotes are a top conversion + trust
 * lever. Until replaced, these are intentionally generic and role-only so no
 * words are put in a named person's mouth.
 */
export type Testimonial = {
  quote: string;
  name: string;
  role: string;
};

export const TESTIMONIALS: Testimonial[] = [
  {
    quote:
      "[Replace with a real Upwork review.] They scoped tightly, communicated clearly across time zones, and shipped a production-ready product faster than we expected.",
    name: "Startup founder",
    role: "United States",
  },
  {
    quote:
      "[Replace with a real Upwork review.] Senior, dependable engineering. The architecture they set up scaled cleanly as we grew.",
    name: "Product lead",
    role: "Europe",
  },
  {
    quote:
      "[Replace with a real Upwork review.] The AI features actually work in production — grounded, accurate, and cost-controlled.",
    name: "CTO",
    role: "United States",
  },
];
